# colors = ['white','black','grey']
#
# upper_c = [c.upper() for c in colors]
#
# upper_colors = []
# for c in colors:
#     upper_colors.append(c.upper())
#
#
#
# my_colors_dict = {c : c[0] for c in colors}
#
#
#
# my_colors_dct = {c : c[0] if(len(c)) else '' for c in colors}
#
# my_colors_dict = {}
# for c in colors:
#     if len(c) > 0:
#         my_colors_dict[0] = c[0]
#     else:
#         my_colors_dict[c] = ''