import base64
import os
import pprint
from functools import lru_cache
import argparse

# encode terns the text to binary

# decode terns the binary to base64 text
#
# url_id = base64.urlsafe_b64encode((r'https://edulabs.co.il/').encode()).decode().strip('=')
# print(url_id)


pprint.pprint(os.environ)

# class


